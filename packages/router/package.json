{
  "name": "@framework/router",
  "version": "0.0.1",
  "description": "File-based routing package",
  "author": {
    "name": "Chris Carrick",
    "email": "chris@crcarrick.dev"
  },
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm clean && tsc -p tsconfig.build.json",
    "clean": "rm -rf ./dist && rm -rf tsconfig.build.tsbuildinfo",
    "tests": "",
    "prepublishOnly": "pnpm build"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
