{
  "name": "@framework/utils",
  "version": "0.0.1",
  "description": "A collection of utilities for @framework",
  "author": {
    "name": "Chris Carrick",
    "email": "chris@crcarrick.dev"
  },
  "main": "dist/index",
  "types": "src/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm clean && tsc -p tsconfig.build.json",
    "clean": "rm -rf ./dist && rm -rf tsconfig.build.tsbuildinfo",
    "tests": "",
    "watch": "pnpm clean && tsc -p tsconfig.build.json --watch",
    "prepublishOnly": "pnpm build"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
