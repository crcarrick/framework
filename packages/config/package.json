{
  "name": "@framework/config",
  "version": "0.0.1",
  "description": "Configuration package for @framework",
  "author": {
    "name": "Chris Carrick",
    "email": "chris@crcarrick.dev"
  },
  "main": "dist/index.js",
  "types": "src/index.ts",
  "files": [
    "dist"
  ],
  "type": "module",
  "scripts": {
    "build": "pnpm clean && tsc -p tsconfig.build.json",
    "clean": "rimraf ./dist && rimraf tsconfig.build.tsbuildinfo",
    "tests": "",
    "watch": "pnpm clean && tsc -p tsconfig.build.json --watch",
    "prepublishOnly": "pnpm build"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@framework/utils": "workspace:*",
    "find-up": "^7.0.0"
  }
}
