{
  "name": "@framework/scripts",
  "version": "0.0.1",
  "description": "A collection of scripts for @framework",
  "author": {
    "name": "Chris Carrick",
    "email": "chris@crcarrick.dev"
  },
  "bin": {
    "framework": "bin/index.js"
  },
  "types": "src/index.ts",
  "files": [
    "bin"
  ],
  "type": "module",
  "scripts": {
    "build": "pnpm clean && tsc -p tsconfig.build.json",
    "clean": "rimraf ./bin && rimraf tsconfig.build.tsbuildinfo",
    "tests": "",
    "watch": "pnpm clean && tsc -p tsconfig.build.json --watch",
    "prepublishOnly": "pnpm build"
  },
  "peerDependencies": {
    "@framework/dev-server": "workspace:^",
    "@framework/server": "workspace:^",
    "@framework/utils": "workspace:^",
    "esbuild": "^0.20.0"
  },
  "devDependencies": {
    "@framework/dev-server": "workspace:^",
    "@framework/server": "workspace:^",
    "@framework/utils": "workspace:^",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3"
  }
}
